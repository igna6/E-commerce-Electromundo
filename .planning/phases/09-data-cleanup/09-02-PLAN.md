---
phase: 09-data-cleanup
plan: 02
type: execute
---

<objective>
Remove all fake/hardcoded data from user-facing UI components — fake reviews, fake discounts, fake installments, hardcoded brands, and placeholder specs.

Purpose: The storefront displays fabricated data (128 reseñas, -15% discounts, 12 cuotas sin interés, hardcoded specs) that destroys customer trust. Removing this fake data is required before the store can go live.
Output: Clean UI components that only display real product data from the API.
</objective>

<execution_context>
@~/.claude/skills/create-plans/workflows/execute-phase.md
@~/.claude/skills/create-plans/templates/summary.md
</execution_context>

<context>
@.planning/BRIEF.md
@.planning/ROADMAP.md
@.planning/phases/09-data-cleanup/09-01-SUMMARY.md
@web/src/sections/products/ProductsPage/components/ProductGridCard.tsx
@web/src/sections/products/ProductDetail/components/ProductInfo.tsx
@web/src/sections/products/ProductDetail/components/ProductDetailTabs.tsx
@web/src/sections/products/ProductsPage/components/FilterSidebar.tsx
@web/src/sections/cart/components/CartSummary.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Clean product cards and list views</name>
  <files>web/src/sections/products/ProductsPage/components/ProductGridCard.tsx</files>
  <action>
    In `ProductGridCard.tsx`, remove all fake/hardcoded data:

    1. **Remove fake discount pricing:**
       - Delete the `originalPrice` calculation (~line 27-30: `product.price * 1.15`)
       - Remove strikethrough "original" price display in both list and grid views
       - Remove the "-15%" discount badge (orange badge ~line 194-196)
       - Keep only the real `product.price` formatted with `formatPrice()`

    2. **Remove fake star ratings and review count:**
       - Remove the hardcoded 4/5 star rating display (~lines 227-238)
       - Remove the "(128)" fake review count
       - Leave the space empty or remove the entire rating row — do NOT replace with another fake element

    3. **Remove hardcoded "Electrónica" category badge:**
       - Remove the hardcoded category badge (~line 85) from list view
       - If product has a real category from the API, display that instead; otherwise omit entirely

    Keep the card layout clean and functional. The card should show: image placeholder (or real image if available), product name (with toTitleCase from 09-01), real price, stock status, and add-to-cart button.
  </action>
  <verify>
    - `cd web && npx tsc --noEmit` passes
    - Grep for "1.15", "15%", "128", "reseñas", "Electrónica" in ProductGridCard.tsx — should find none
  </verify>
  <done>ProductGridCard shows only real data: name, price, stock. No fake discounts, ratings, reviews, or hardcoded category badges.</done>
</task>

<task type="auto">
  <name>Task 2: Clean product detail page</name>
  <files>web/src/sections/products/ProductDetail/components/ProductInfo.tsx, web/src/sections/products/ProductDetail/components/ProductDetailTabs.tsx</files>
  <action>
    **In `ProductInfo.tsx`:**

    1. **Remove fake rating display** (~line 41): Delete the "(4.2) - 128 reseñas" text and star icons
    2. **Remove fake discount** (~lines 53-56): Delete `formatPrice(Math.round(product.price * 1.15))` strikethrough price and "-15%" badge. Show only the real price.
    3. **Remove fake installments** (~line 66): Delete the "12 cuotas sin interés de $X" text entirely
    4. **Remove fake trust badges** (~lines 78-112): Delete the hardcoded feature badges ("Garantía 12 meses", "Envío gratis", "Compra protegida", "Devolución gratis"). These are promises the store hasn't committed to yet.
    5. **Fix fallback description** (~line 74): Remove the fake generic description "Producto de alta calidad con garantía oficial. Envío a todo el país." — if there's no description, show nothing or a simple "Sin descripción disponible."

    **In `ProductDetailTabs.tsx`:**

    1. **Remove fake "Reseñas" tab** entirely — the hardcoded reviews with "Juan D." and fabricated text (~lines 62-95). Remove the tab and its content. Do NOT replace with an empty reviews section.
    2. **Remove fake "Especificaciones" tab** — the hardcoded specs (Marca: "Genérica", Modelo: "2024", Garantía: "12 meses", País: "Argentina" ~lines 33-59). Remove this tab entirely. Real specs will be added in a future phase when the data model supports them.
    3. **Keep the "Descripción" tab** — this shows real product data from the API. If it's the only tab remaining, render it without the tab UI (just show the description directly).
    4. **Simplify or remove "Envío y Devoluciones" tab** (~lines 98-118) — the hardcoded shipping info ("Envío Gratis over $50,000", "3-5 días hábiles", "30 días devolución") is not confirmed store policy. Remove this tab.

    After cleanup, if only the description tab remains, simplify the component to just render the product description without tab navigation overhead.
  </action>
  <verify>
    - `cd web && npx tsc --noEmit` passes
    - Grep for "reseñas", "128", "4.2", "1.15", "15%", "cuotas", "sin interés", "Genérica", "Juan D", "Envío gratis", "Compra protegida", "Devolución gratis" in both files — should find none
  </verify>
  <done>Product detail page shows only real data: name, real price, description, stock status. No fake reviews, ratings, discounts, installments, specs, shipping promises, or trust badges.</done>
</task>

<task type="auto">
  <name>Task 3: Clean cart summary and filter sidebar</name>
  <files>web/src/sections/cart/components/CartSummary.tsx, web/src/sections/products/ProductsPage/components/FilterSidebar.tsx</files>
  <action>
    **In `CartSummary.tsx`:**

    1. **Remove fake installments** (~lines 74-78): Delete the "12 cuotas sin interés de $X" financing info box. This is not a real payment option.
    2. **Remove hardcoded trust badges** (~lines 95-136): Delete "Compra Segura", "30 Días Devolución", "Pago Seguro", "Garantía" badges. These are unverified promises.
    3. Keep the cart functional: item list, quantities, subtotal, total, and the checkout/WhatsApp button.

    **In `FilterSidebar.tsx`:**

    1. **Remove hardcoded brands array** (~lines 16-25): Delete the static `brands` array ('Apple', 'Samsung', 'Sony', 'LG', 'HP', 'Dell', 'Logitech', 'JBL').
    2. **Remove the brand filter checkboxes UI** (~lines 112-127) that maps over the hardcoded brands.
    3. **Keep category filters** if they use real API data. If the categories array (~lines 6-14) is also hardcoded with fake data ('electronics', 'computing', etc. with emoji icons), remove it too — these don't match real database categories. Keep only the category filter if it fetches from the API.
    4. Dynamic brand filters from real DB data will be built in Phase 11 — for now, just remove the fake ones.
  </action>
  <verify>
    - `cd web && npx tsc --noEmit` passes
    - Grep for "cuotas", "sin interés", "Compra Segura", "Pago Seguro", "Apple", "Samsung", "Sony", "Logitech" in both files — should find none
  </verify>
  <done>Cart shows only real data (items, prices, total, checkout). Filter sidebar has no hardcoded brands. No fake installments or trust badges anywhere.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `cd web && npx tsc --noEmit` passes
- [ ] `cd web && npm run build` succeeds
- [ ] No fake data strings remain: grep entire `web/src/` for "reseñas", "128 reseñas", "cuotas sin interés", "1.15", "-15%", "Juan D", "Genérica", "Compra protegida", "Envío gratis" (as hardcoded badge text)
- [ ] Product cards render cleanly without visual breakage
- [ ] Product detail page shows only real data
- [ ] Cart summary is functional without fake elements
</verification>

<success_criteria>
- Zero fake/hardcoded data visible in any user-facing UI component
- No fake reviews, ratings, discounts, installments, specs, or trust badges
- No hardcoded brand filters
- All components typecheck and build cleanly
- UI remains functional and visually coherent after removals
- Phase 9 complete, ready for Phase 10 (Images & Categories)
</success_criteria>

<output>
After completion, create `.planning/phases/09-data-cleanup/09-02-SUMMARY.md`:

# Phase 9 Plan 2: Remove Fake UI Elements Summary

**[One-liner describing what shipped]**

## Accomplishments
- [Key outcomes]

## Files Created/Modified
- `path/to/file` - Description

## Decisions Made
[Key decisions and rationale, or "None"]

## Issues Encountered
[Problems and resolutions, or "None"]

## Next Step
Phase 9 complete. Ready for Phase 10 (Images & Categories).
</output>
