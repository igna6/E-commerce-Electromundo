---
phase: 08-checkout-simplification
plan: 03
type: execute
depends_on: 08-02
---

<objective>
Add WhatsApp integration to order confirmation: generate a "Send via WhatsApp" button that opens WhatsApp with the order text pre-filled, and clean up the confirmation page to match the simplified order flow.

Purpose: The order text is generated specifically to be shared with the store. WhatsApp is the standard business communication channel in Argentina. This closes the loop: customer confirms order → sends it to the store via WhatsApp → store processes it.

Output: WhatsApp button on order confirmation page. Simplified confirmation display (no address/shipping/payment sections). Store phone number in config.
</objective>

<execution_context>
@~/.claude/plugins/cache/taches-cc-resources/taches-cc-resources/1.0.0/skills/create-plans/workflows/execute-phase.md
@~/.claude/plugins/cache/taches-cc-resources/taches-cc-resources/1.0.0/skills/create-plans/templates/summary.md
</execution_context>

<context>
@BRIEF.md

Key files to load:
@web/src/sections/order-confirmation/OrderConfirmationPage.tsx — Current confirmation page with copy button, address display, shipping/payment
@web/src/constants/config.ts — App configuration (API_URL)
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add store WhatsApp number to config</name>
  <files>web/src/constants/config.ts</files>
  <action>
Add a constant for the store's WhatsApp number. Use the international format without + or spaces (WhatsApp API requirement).

```typescript
// Store WhatsApp number in international format (no + or spaces)
// Example: Argentina country code (54) + area code + number
export const STORE_WHATSAPP_NUMBER = '5491112345678' // TODO: Replace with actual store number
```

Add a TODO comment so the user knows to replace it with their real number. The format must be digits only: country code + number (e.g., `5491112345678` for Argentina +54 9 11 1234-5678).
  </action>
  <verify>File has the constant exported.</verify>
  <done>WhatsApp number constant available for use. Clearly marked as TODO for the user to update.</done>
</task>

<task type="auto">
  <name>Task 2: Simplify and enhance order confirmation page</name>
  <files>web/src/sections/order-confirmation/OrderConfirmationPage.tsx</files>
  <action>
Rewrite the confirmation page with these changes:

**Remove:**
- "Direccion de Envio" section (no delivery)
- Shipping cost line from totals (always 0)
- The "Te enviamos un correo a..." text (no email system exists — it's misleading)

**Simplify the order summary:**
- Contact info: name, email, phone (single block, not 2-column grid)
- Show "Retiro en Sucursal" as the delivery method (simple text, not a section)
- Items list (keep as-is)
- Totals: subtotal, IVA (21%), total (no shipping line)

**Add WhatsApp button** — this is the PRIMARY call-to-action:
- Import `STORE_WHATSAPP_NUMBER` from config
- Create a WhatsApp deep link: `https://wa.me/{number}?text={encodedText}`
- The `text` parameter should be `encodeURIComponent(order.orderText)`
- Button: green background (#25D366 WhatsApp green), white text, WhatsApp icon
- Label: "Enviar Pedido por WhatsApp"
- Opens in new tab (`target="_blank"`)
- Place it prominently — right after the success message, BEFORE the order summary
- Also keep the "Copiar" button but make it secondary

**Button layout (in order of prominence):**
1. "Enviar Pedido por WhatsApp" — primary, large, green, with WhatsApp icon
2. "Copiar Pedido" — secondary outline button (existing copy functionality)
3. "Volver al Inicio" / "Seguir Comprando" — tertiary, at bottom

**WhatsApp icon:** Use a simple SVG inline or from lucide-react. WhatsApp SVG path:
```
M17.472 14.382c-.297-.149-1.758-.867-2.03-.967-.273-.099-.471-.148-.67.15-.197.297-.767.966-.94 1.164-.173.199-.347.223-.644.075-.297-.15-1.255-.463-2.39-1.475-.883-.788-1.48-1.761-1.653-2.059-.173-.297-.018-.458.13-.606.134-.133.298-.347.446-.52.149-.174.198-.298.298-.497.099-.198.05-.371-.025-.52-.075-.149-.669-1.612-.916-2.207-.242-.579-.487-.5-.669-.51-.173-.008-.371-.01-.57-.01-.198 0-.52.074-.792.372-.272.297-1.04 1.016-1.04 2.479 0 1.462 1.065 2.875 1.213 3.074.149.198 2.096 3.2 5.077 4.487.709.306 1.262.489 1.694.625.712.227 1.36.195 1.871.118.571-.085 1.758-.719 2.006-1.413.248-.694.248-1.289.173-1.413-.074-.124-.272-.198-.57-.347z
```

**Handle missing WhatsApp number gracefully:** If `STORE_WHATSAPP_NUMBER` is the placeholder `'5491112345678'`, still show the button (it'll open WhatsApp with a random number — user will update it). Do not add conditional logic for this.
  </action>
  <verify>`cd web && npm run build` succeeds. Order confirmation page renders with WhatsApp button.</verify>
  <done>Order confirmation has prominent WhatsApp button, simplified layout, no misleading email/address/shipping info.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>Complete checkout simplification: single-step form → order confirmation with WhatsApp button</what-built>
  <how-to-verify>
    1. Run: `cd web && npm run dev` (and server)
    2. Add product to cart → go to checkout
    3. Fill name, email, phone → click "Confirmar Pedido"
    4. Verify: redirects to confirmation page
    5. Verify: "Enviar Pedido por WhatsApp" button is prominent and green
    6. Click WhatsApp button → verify it opens wa.me with the order text pre-filled
    7. Verify: "Copiar Pedido" button still works
    8. Verify: no address section, no shipping cost, no payment method shown
    9. Verify: order text in the pre block is clean (contact info, products, totals only)
    10. Check on mobile viewport — WhatsApp button should be full-width and easy to tap
  </how-to-verify>
  <resume-signal>Type "approved" if the flow works, or describe issues to fix. Also: provide the real store WhatsApp number to replace the placeholder.</resume-signal>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `cd web && npm run build` — no errors
- [ ] WhatsApp button opens correct wa.me URL with encoded order text
- [ ] Order text is clean (no payment method, conditional address, simplified)
- [ ] Confirmation page shows: success message → WhatsApp button → order summary → order text → navigation
- [ ] Copy to clipboard still works
- [ ] No "Te enviamos un correo" text (no email system)
- [ ] No shipping address section
- [ ] No shipping cost in totals
- [ ] Mobile responsive — WhatsApp button is prominent and tappable
</verification>

<success_criteria>
- Customer completes order → sees WhatsApp button as primary action
- One tap sends the full order text to the store's WhatsApp
- Order text is clean, readable, and includes all necessary info
- Confirmation page has no misleading information (no email promises, no delivery address)
- Copy fallback still available
</success_criteria>

<output>
After completion, create `.planning/phases/08-checkout-simplification/08-03-SUMMARY.md`
</output>
