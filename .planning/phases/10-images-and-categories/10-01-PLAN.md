---
phase: 10-images-and-categories
plan: 01
type: execute
---

<objective>
Make product images practical to use at scale — extend CSV import to accept image URLs and fix the product detail gallery hack.

Purpose: The image column and admin form already exist, but there's no way to bulk-assign images to ~160 products, and the detail page duplicates a single image 4 times to simulate a gallery. This plan makes images usable.
Output: CSV import handles image URLs, product detail page displays single images cleanly.
</objective>

<execution_context>
@~/.claude/skills/create-plans/workflows/execute-phase.md
@~/.claude/skills/create-plans/templates/summary.md
</execution_context>

<context>
@.planning/BRIEF.md
@.planning/ROADMAP.md
@.planning/phases/09-data-cleanup/09-01-SUMMARY.md
@server/src/services/admin/importProducts.service.ts
@server/src/entities/products.ts
@web/src/sections/products/ProductDetail/ProductDetail.tsx
@web/src/sections/products/ProductDetail/components/ProductImageGallery.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extend CSV import to accept image URL column</name>
  <files>server/src/services/admin/importProducts.service.ts</files>
  <action>
    Follow the same pattern used for the optional price column (added in 09-01):

    1. Add `imageIdx` detection using header matching for: "imagen", "image", "foto", "url_imagen", "url_image", "img", "picture", "photo" (same normalize pattern as other headers)
    2. Image column is OPTIONAL — do not throw if missing
    3. In the data row parsing loop: extract image URL string, trim whitespace. Basic validation: must start with "http://" or "https://". If invalid, skip (don't set image for that row, log to errors array)
    4. Add `image` as optional field to `validProducts` array type
    5. In the transaction:
       - For NEW products (insert): include image if available
       - For EXISTING products (update): update image if CSV provides a non-empty valid URL. Do NOT overwrite an existing image with empty/null.
    6. Add `imageUpdated: number` counter to return value

    Do NOT change the CSV parser. Do NOT add file upload — URL-only for now.
  </action>
  <verify>
    - `cd server && npx tsc --noEmit` passes
    - Import still works with CSVs without image column (backwards compatible)
  </verify>
  <done>Import service accepts optional image URL column. New products get image if provided. Existing products get image updated only when CSV provides a valid URL. TypeScript compiles clean.</done>
</task>

<task type="auto">
  <name>Task 2: Fix product detail gallery duplication hack</name>
  <files>web/src/sections/products/ProductDetail/ProductDetail.tsx, web/src/sections/products/ProductDetail/components/ProductImageGallery.tsx</files>
  <action>
    1. In `ProductDetail.tsx`: Remove the hack `const images = product?.image ? [product.image, product.image, product.image, product.image] : []`. Instead, pass a single-element array: `const images = product?.image ? [product.image] : []`

    2. In `ProductImageGallery.tsx`: Ensure the component handles single-image gracefully:
       - If `images.length === 1`: Show the image full-size without thumbnail strip below (thumbnails are useless with 1 image)
       - If `images.length === 0`: Show a clean placeholder (e.g., a Package/ImageOff icon with "Sin imagen" text, styled consistently with the product card placeholders)
       - If `images.length > 1` (future multi-image): Keep existing thumbnail gallery behavior

    Do NOT add multi-image database support (product_images table) — that's out of scope for v1.0.
    Do NOT change the image aspect ratio or sizing — keep existing layout dimensions.
  </action>
  <verify>
    - `cd web && npx tsc --noEmit` passes
    - Grep for the duplication hack `product.image, product.image` — should find none
  </verify>
  <done>Product detail page shows single image cleanly without duplication. Empty state shows placeholder. Gallery component handles 0, 1, or N images correctly.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `cd server && npx tsc --noEmit` passes
- [ ] `cd web && npx tsc --noEmit` passes
- [ ] No image duplication hack remains in ProductDetail.tsx
- [ ] Import service handles CSVs with and without image column
</verification>

<success_criteria>
- CSV import accepts optional image URL column with validation
- Product detail page displays single image without duplication
- Empty image state shows clean placeholder
- Both server and web typecheck cleanly
</success_criteria>

<output>
After completion, create `.planning/phases/10-images-and-categories/10-01-SUMMARY.md`:

# Phase 10 Plan 1: Product Images Summary

**[One-liner describing what shipped]**

## Accomplishments
- [Key outcomes]

## Files Created/Modified
- `path/to/file` - Description

## Decisions Made
[Key decisions and rationale, or "None"]

## Issues Encountered
[Problems and resolutions, or "None"]

## Next Step
Ready for 10-02-PLAN.md (Category hierarchy)
</output>
