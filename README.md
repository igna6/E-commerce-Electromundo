# E-commerce Electromundo

e-commerce project

## Project Structure

### Server (`/server`)

The backend server built with Express.js and TypeScript.

```
cd ./server
yarn dev
```

### Web (`/web`)

The frontend application built with React and TanStack Router.

```
cd ./web
yarn dev
```
